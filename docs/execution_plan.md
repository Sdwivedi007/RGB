# MVP Execution Plan

1. **Docker stack + Auth**
   - PostGIS, Redis, MinIO, API, worker, tile server, web
2. **Clients/Grounds + Home Map routing**
   - Display all grounds on map with basic detail view
3. **Ortho upload + COG + tiles**
   - Upload, COG conversion, TiTiler tiles
4. **Draw tools + Analyze endpoint + 5 indices**
   - AOI create/edit, compute basic indices
5. **Expand to 17 indices + compare**
   - JSON registry formulas, compare UX (opacity/swipe/split)
6. **Save AOI + PDF/CSV export**
   - Store AOI, generate reports
7. **Potree 3D module**
   - Pointcloud conversion + viewer

